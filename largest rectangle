int sz=height.size();
{int maxArea=0;
for(i=0;i<sz;i++)
{
int l=i,r=i;
while(left>=0&&height[l]<=height[i])
--l;
while(r<sz&&height[r]>=height[i])
r++;
int area=height[i]*(r-l+1)
maxarea=max(maxarea,area);
}
return maxarea;
}
